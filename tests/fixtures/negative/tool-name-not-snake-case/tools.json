{
  "tools": [
    {
      "name": "fetch_user",
      "description": "Fetch a user by id.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": { "type": "string" }
        },
        "required": ["id"]
      }
    },
    {
      "name": "send_message",
      "description": "Post a message to a channel.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "channel": { "type": "string" },
          "body": { "type": "string" }
        }
      }
    }
  ]
}
